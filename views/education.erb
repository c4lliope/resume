<section id="education">
  <h2>Education</h2>

  <% @data['education'].each do |topic| %>
    <div class="topic <%= topic['tags'] %>">
      <span class="name"><%= topic['name'] %></span>
      <span class="gpa"><%= topic['gpa'] %></span>
    </div>
  <% end %>
</section>
